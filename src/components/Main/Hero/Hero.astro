---
import ButtonMain from "../../ButtonMain.astro";
import ButtonSub from "../../ButtonSub.astro";
import LazyImage from "../../LazyImage.astro";
import OptimizedImage from "../../OptimizedImage.astro";
import AOS from "../../AOS.astro";
import { getPsychologyServiceSchema } from "../../../data/siteSchema";

// SEO optimization with structured data
const jsonLd = getPsychologyServiceSchema();
---

<!-- Import optimized AOS -->
<AOS />

<section class="w-full px-3 sm:px-4 py-20 sm:py-32 flex flex-col items-center justify-center overflow-hidden relative" aria-label="Bienvenida al servicio de psicología profesional">
  <!-- Fondos y decoración con mejor contraste -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute inset-0 bg-gradient-radial from-blue-500/10 to-transparent opacity-80"></div>
    <div class="absolute top-1/4 -left-1/4 w-1/2 h-1/2 bg-gradient-to-r from-cyan-500/15 to-blue-500/15 blur-[100px] rounded-full animate-blob animation-delay-2000"></div>
    <div class="absolute bottom-1/3 -right-1/4 w-1/2 h-1/2 bg-gradient-to-l from-purple-500/15 to-blue-500/15 blur-[100px] rounded-full animate-blob animation-delay-4000"></div>
    <div class="absolute w-full h-full bg-[url('/img/noise.svg')] opacity-[0.03] mix-blend-overlay"></div>
  </div>

  <div class="w-full max-w-6xl mx-auto flex flex-col items-center z-10">
    <!-- Título y texto optimizados para SEO y accesibilidad -->
    <div data-aos="fade-up" data-aos-duration="800" class="text-center mb-12 sm:mb-16 w-full max-w-4xl">
      <h1 class="font-extrabold text-4xl sm:text-5xl md:text-6xl lg:text-7xl tracking-tight mb-6 sm:mb-8 text-transparent">
        <span class="inline-block relative">
          <span class="absolute inset-0 bg-gradient-to-r from-blue-600 via-blue-400 to-cyan-400 blur-[40px] opacity-30"></span>
          <span class="relative bg-gradient-to-r from-blue-600 via-blue-400 to-cyan-400 bg-clip-text">Tu bienestar mental</span>
        </span>
        <br />
        <span class="inline-block text-gray-100 mt-2">es nuestra prioridad</span>
      </h1>
      <h2 class="text-base sm:text-2xl text-slate-300 leading-relaxed max-w-2xl mx-auto mb-8 sm:mb-10">
        Somos un equipo de psicólogos especializados comprometidos con tu salud mental.
        <span class="block mt-8 text-base sm:text-2xl">Ofrecemos terapias personalizadas en un entorno seguro y confidencial.</span>
      </h2>
      <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center items-center">
        <ButtonMain to="#appointment" size="lg" hasArrow={true}>Solicitar Cita</ButtonMain>
        <ButtonSub to="#services" size="lg" hasIcon={true} iconName="Info">Nuestros Servicios</ButtonSub>
      </div>
    </div>
    
    <!-- Imagen principal y decoraciones optimizadas -->
    <div data-aos="zoom-in" data-aos-duration="1000" data-aos-delay="200" class="relative w-full flex justify-center mt-8 group">
      <div class="absolute inset-0 flex justify-center items-center pointer-events-none" aria-hidden="true">
        <div class="hero-radial-bg"></div>
        <div class="hero-halo-pulse"></div>
      </div>
      
      <div class="relative flex justify-center items-center max-w-[700px] w-full">
        <!-- Tarjetas flotantes animadas con mejor contraste -->
        <div class="absolute top-0 left-0 sm:-left-16 md:-left-24 -translate-y-1/4 z-20 hidden sm:block">
          <div class="glassmorphism p-3 md:p-4 rounded-xl shadow-xl animate-float-slow transform rotate-[-5deg]">
            <div class="flex items-center space-x-3">
              <div class="w-9 h-9 md:w-10 md:h-10 bg-blue-500/20 rounded-full flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 md:h-6 md:w-6 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div class="text-left">
                <div class="text-xs md:text-sm font-medium text-gray-200">Más de 99%</div>
                <div class="text-xs text-gray-300">tasa de satisfacción</div>
              </div>
            </div>
          </div>
        </div>
        
        <div class="absolute -bottom-12 sm:-bottom-8 right-0 sm:-right-12 md:-right-20 z-20 hidden sm:block">
          <div class="glassmorphism p-3 md:p-4 rounded-xl shadow-xl animate-float-medium transform rotate-[3deg]">
            <div class="flex items-center space-x-3">
              <div class="w-9 h-9 md:w-10 md:h-10 bg-blue-500/20 rounded-full flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 md:h-6 md:w-6 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div class="text-left">
                <div class="text-xs md:text-sm font-medium text-gray-200">Primera sesión</div>
                <div class="text-xs text-gray-300">evaluación con descuento</div>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Imagen principal con mejor accesibilidad y perf -->
        <div class="glassmorphism-lg rounded-2xl overflow-hidden shadow-2xl border border-white/10 transform transition-all duration-500 group-hover:scale-[1.02] group-hover:shadow-blue-500/20">
          <OptimizedImage
            src="/img/illustrations/hero.svg"
            class="block dark:hidden w-full max-w-[450px] sm:max-w-[550px] md:max-w-[600px] relative z-10"
            alt="Psicólogos profesionales brindando atención personalizada a pacientes en una sesión terapéutica"
            loading="eager"
            width={600}
            height={450}
            fetchpriority="high"
          />
          <OptimizedImage
            src="/img/illustrations/hero-dark.svg"
            class="hidden dark:block w-full max-w-[450px] sm:max-w-[550px] md:max-w-[600px] relative z-10"
            alt="Psicólogos profesionales brindando atención personalizada a pacientes en una sesión terapéutica"
            loading="eager"
            width={600}
            height={450}
            fetchpriority="high"
          />
        </div>
        
        <!-- Decoración de emojis con mejor accesibilidad -->
        <div class="absolute left-3 h-8 w-8 sm:h-10 sm:w-10 flex items-center justify-center rounded-full text-lg sm:text-xl glass-emoji shadow-lg animate-float-slow z-20 emoji-glow" aria-hidden="true">
          🧠
        </div>
        <div class="absolute top-12 left-10 h-6 w-6 sm:h-8 sm:w-8 flex items-center justify-center rounded-full text-sm sm:text-lg glass-emoji shadow-lg animate-emoji-spin-scale z-20 emoji-glow text-white" aria-hidden="true">
          🌱
        </div>
        <div class="absolute bottom-12 right-5 h-8 w-8 sm:h-12 sm:w-12 flex items-center justify-center rounded-full text-xl sm:text-2xl glass-emoji shadow-lg animate-float-fast z-20 emoji-glow" aria-hidden="true">
          💭
        </div>
        <div class="absolute -top-4 left-1/2 -translate-x-1/2 h-8 w-8 sm:h-12 sm:w-12 flex items-center justify-center rounded-full text-xl sm:text-2xl glass-emoji shadow-lg animate-pulse z-20 emoji-glow" aria-hidden="true">
          ❤️‍🩹
        </div>
      </div>
    </div>
    
    <!-- Estadísticas rápidas mejoradas -->
    <div data-aos="fade-up" data-aos-duration="800" data-aos-delay="400" class="grid grid-cols-2 md:grid-cols-4 gap-4 sm:gap-6 mt-16 w-full max-w-4xl">
      <div class="glassmorphism p-4 sm:p-6 rounded-xl text-center transform transition-all duration-300 hover:scale-105 hover:shadow-blue-500/10">
        <div class="text-blue-400 text-2xl sm:text-3xl font-bold mb-1">+500</div>
        <div class="text-gray-300 text-xs sm:text-sm">Pacientes atendidos</div>
      </div>
      <div class="glassmorphism p-4 sm:p-6 rounded-xl text-center transform transition-all duration-300 hover:scale-105 hover:shadow-blue-500/10">
        <div class="text-blue-400 text-2xl sm:text-3xl font-bold mb-1">99%</div>
        <div class="text-gray-300 text-xs sm:text-sm">Satisfacción</div>
      </div>
      <div class="glassmorphism p-4 sm:p-6 rounded-xl text-center transform transition-all duration-300 hover:scale-105 hover:shadow-blue-500/10">
        <div class="text-blue-400 text-2xl sm:text-3xl font-bold mb-1">2</div>
        <div class="text-gray-300 text-xs sm:text-sm">Especialistas</div>
      </div>
      <div class="glassmorphism p-4 sm:p-6 rounded-xl text-center transform transition-all duration-300 hover:scale-105 hover:shadow-blue-500/10">
        <div class="text-blue-400 text-2xl sm:text-3xl font-bold mb-1">+10</div>
        <div class="text-gray-300 text-xs sm:text-sm">Años de experiencia</div>
      </div>
    </div>
    
    <!-- Testimonios destacados mejorados -->
    <div data-aos="fade-up" data-aos-duration="800" data-aos-delay="200" class="w-full max-w-2xl mx-auto my-12">
      <div class="glassmorphism p-5 sm:p-6 rounded-xl shadow-xl backdrop-blur-md border border-white/5">
        <div class="flex justify-between items-center mb-4">
          <h3 class="font-bold text-gray-100 text-lg uppercase tracking-wider">Testimonios</h3>
          <span class="text-xs bg-blue-500/30 text-blue-300 font-medium px-2 py-1 rounded-full">5 nuevos</span>
        </div>
        
        <div class="mb-4">
          <p class="text-gray-300 text-base italic leading-relaxed">"La terapia ha transformado mi vida. Estoy profundamente agradecida por la atención y el apoyo recibido durante todo el proceso."</p>
          <div class="flex items-center mt-3">
            <div class="flex-shrink-0 mr-3">
              <OptimizedImage 
                class="w-10 h-10 rounded-full ring-2 ring-blue-400/30" 
                src="https://media.cssninja.io/shuriken/avatars/2.svg" 
                alt="Fotografía de Ana R., paciente satisfecha"
                width={40}
                height={40}
                loading="lazy"
              />
            </div>
            <div>
              <p class="text-sm font-medium text-gray-200">Ana R.</p>
              <p class="text-xs text-blue-400">hace 2 días</p>
            </div>
          </div>
        </div>
        
        <div class="flex flex-wrap items-center gap-2">
          <span class="text-sm text-gray-400">Más testimonios:</span>
          <div class="flex -space-x-2">
            <OptimizedImage class="w-8 h-8 rounded-full ring-2 ring-gray-800" src="https://media.cssninja.io/shuriken/avatars/4.svg" alt="Fotografía de paciente" width={32} height={32} />
            <OptimizedImage class="w-8 h-8 rounded-full ring-2 ring-gray-800" src="https://media.cssninja.io/shuriken/avatars/3.svg" alt="Fotografía de paciente" width={32} height={32} />
            <OptimizedImage class="w-8 h-8 rounded-full ring-2 ring-gray-800" src="https://media.cssninja.io/shuriken/avatars/6.svg" alt="Fotografía de paciente" width={32} height={32} />
            <a href="#testimonios" class="flex items-center justify-center w-8 h-8 rounded-full bg-blue-500/20 text-blue-400 font-medium text-xs ring-2 ring-gray-800 hover:bg-blue-500/30 transition-colors">
              +10
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Scroll indicator mejorado -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 flex flex-col items-center text-gray-300">
    <span class=" mb-2 font-medium bg-blue-500/30 text-blue-300  px-2 py-1 rounded-full animate-pulse hover:scale-105 transition-all duration-300 cursor-pointer ">Descubre más</span>
    <div class="w-6 h-10 border-2 border-gray-400 rounded-full flex justify-center">
      <div class="w-1.5 h-1.5 bg-blue-400 rounded-full animate-bounce-slow mt-2"></div>
    </div>
  </div>
  
  <!-- Botón flotante de chat -->
  <button 
    id="chat-button"
    aria-label="Abrir chat de asistencia psicológica" 
    class="fixed bottom-6 right-6 z-50 w-14 h-14 rounded-full bg-gradient-to-r from-blue-600 to-cyan-400 shadow-lg flex items-center justify-center transform transition-all duration-300 hover:scale-110 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-offset-2 focus:ring-offset-gray-900"
  >
    <span class="sr-only">Iniciar chat con un psicólogo</span>
    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
    </svg>
  </button>
</section>

<!-- Estructurados datos para SEO -->
<script type="application/ld+json" set:html={JSON.stringify(jsonLd)}></script>

<style>
  .hero-radial-bg {
    position: absolute;
    width: 100%;
    max-width: 800px;
    height: 100%;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.15) 0%, rgba(16, 24, 39, 0) 70%);
    opacity: 0.8;
  }
  
  .hero-halo-pulse {
    position: absolute;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.15) 0%, rgba(16, 24, 39, 0) 70%);
    opacity: 0.5;
    border-radius: 50%;
    animation: pulse 8s ease-in-out infinite alternate;
  }
  
  .glassmorphism {
    background: rgba(30, 41, 59, 0.4);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.05);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
  }
  
  .glassmorphism-lg {
    background: rgba(30, 41, 59, 0.3);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.3);
    transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }
  
  .glassmorphism-lg:hover {
    transform: translateY(-5px);
    box-shadow: 0 30px 60px -15px rgba(59, 130, 246, 0.3);
  }
  
  .glassmorphism-lg:hover {
    transform: translateY(-5px);
    box-shadow: 0 30px 60px -15px rgba(59, 130, 246, 0.3);
  }
  
  .glass-emoji {
    backdrop-filter: blur(4px);
    background: rgba(30, 41, 59, 0.4);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  
  .emoji-glow {
    box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
  }
  
  /* Chat button pulse animation */
  #chat-button::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: 9999px;
    background: linear-gradient(90deg, #3b82f6, #06b6d4);
    animation: pulse-border 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  
  @keyframes pulse-border {
    0%, 100% {
      opacity: 0.7;
      transform: scale(1);
    }
    50% {
      opacity: 0;
      transform: scale(1.3);
    }
  }
  
  @keyframes pulse {
    0% {
      transform: scale(0.8);
      opacity: 0.3;
    }
    100% {
      transform: scale(1.2);
      opacity: 0.5;
    }
  }
  
  @keyframes float-slow {
    0% {
      transform: translateY(0px) rotate(-5deg);
    }
    100% {
      transform: translateY(-15px) rotate(-3deg);
    }
  }
  
  @keyframes float-medium {
    0% {
      transform: translateY(0px) rotate(3deg);
    }
    100% {
      transform: translateY(-10px) rotate(5deg);
    }
  }
  
  @keyframes float-fast {
    0% {
      transform: translateY(0px);
    }
    100% {
      transform: translateY(-8px);
    }
  }
  
  .animate-float-slow {
    animation: float-slow 6s ease-in-out infinite alternate;
  }
  
  .animate-float-medium {
    animation: float-medium 4s ease-in-out infinite alternate;
  }
  
  .animate-float-fast {
    animation: float-fast 3s ease-in-out infinite alternate;
  }
  
  .animate-bounce-slow {
    animation: bounce 2s infinite;
  }
  
  .animate-blob {
    animation: blob 7s infinite;
  }
  
  .animation-delay-2000 {
    animation-delay: 2s;
  }
  
  .animation-delay-4000 {
    animation-delay: 4s;
  }
  
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }
  
  .animate-emoji-spin-scale {
    animation: emoji-spin-scale 5s ease-in-out infinite alternate;
  }
  
  @keyframes emoji-spin-scale {
    0% {
      transform: rotate(-5deg) scale(0.9);
    }
    100% {
      transform: rotate(5deg) scale(1.1);
    }
  }

  /* Notification badge animation */
  .chat-notification:not(.hidden) {
    animation: notification-pulse 2s infinite;
  }

  @keyframes notification-pulse {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.2);
    }
    100% {
      transform: scale(1);
    }
  }

  /* Mejora para lectores de pantalla - ocultar animaciones */
  @media (prefers-reduced-motion: reduce) {
    .animate-float-slow,
    .animate-float-medium,
    .animate-float-fast,
    .animate-emoji-spin-scale,
    .animate-blob,
    .animate-bounce-slow,
    .hero-halo-pulse,
    #chat-button::before,
    .chat-notification:not(.hidden) {
      animation: none;
    }
    
    .glassmorphism:hover,
    .group:hover .glassmorphism-lg,
    .glassmorphism-lg:hover {
      transform: none;
    }
  }
  
  /* Ajustes para diferentes dispositivos */
  @media (max-width: 640px) {
    .glassmorphism-lg {
      max-width: 90vw;
    }
  }
</style>

<script>
  // Carga optimizada de AOS para animaciones al scroll
  document.addEventListener('DOMContentLoaded', () => {
    // Verificar si AOS ya existe
    if (typeof (window as any).AOS === 'undefined') {
      // Cargar estilos CSS
      const aosCSS = document.createElement('link');
      aosCSS.rel = 'stylesheet';
      aosCSS.href = 'https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css';
      document.head.appendChild(aosCSS);
      
      // Cargar script con carga diferida para mejorar el rendimiento inicial
      setTimeout(() => {
        const aosScript = document.createElement('script');
        aosScript.src = 'https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js';
        aosScript.async = true;
        aosScript.onload = () => {
          // Inicializar AOS con configuración optimizada
          (window as any).AOS.init({
            duration: 800,
            once: true,
            offset: 50,
            disable: window.matchMedia('(prefers-reduced-motion: reduce)').matches
          });
        };
        document.body.appendChild(aosScript);
      }, 100); // Pequeño retraso para priorizar el contenido principal
    } else {
      // Si AOS ya existe, solo inicializarlo
      (window as any).AOS.init();
    }
  });

  // Añadir evento para mejorar accesibilidad del scroll indicator
  document.addEventListener('DOMContentLoaded', () => {
    const scrollIndicator = document.querySelector('.bottom-8.left-1\\/2');
    if (scrollIndicator) {
      scrollIndicator.addEventListener('click', () => {
        const nextSection = document.querySelector('section:nth-of-type(2)');
        if (nextSection) {
          nextSection.scrollIntoView({ behavior: 'smooth' });
        }
      });
    }
    
    // Funcionalidad del botón de chat
    setupChatButton();
  });
  
  // Configuración del chat
  function setupChatButton() {
    const chatButton = document.getElementById('chat-button');
    if (!chatButton) return;
    
    // Crear modal de chat
    const chatModal = document.createElement('div');
    chatModal.id = 'chat-modal';
    chatModal.className = 'fixed bottom-24 right-6 z-50 w-80 sm:w-96 h-96 bg-slate-800 rounded-xl shadow-2xl border border-slate-700 flex flex-col overflow-hidden transform transition-all duration-300 scale-0 origin-bottom-right';
    chatModal.setAttribute('aria-hidden', 'true');
    
    // Estructura interna del chat
    chatModal.innerHTML = `
      <div class="bg-gradient-to-r from-blue-600 to-cyan-400 p-4 flex items-center justify-between">
        <h3 class="text-white font-medium">Chat con psicólogo</h3>
        <button id="close-chat" class="text-white hover:bg-white/10 rounded-full p-1 transition-colors cursor-pointer">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
          <span class="sr-only">Cerrar chat</span>
        </button>
      </div>
      <div class="flex-1 p-4 overflow-y-auto bg-slate-900/50" id="chat-messages">
        <div class="chat-message assistant mb-4">
          <div class="flex items-end">
            <div class="flex flex-col space-y-2 text-sm max-w-xs mx-2 order-2 items-start">
              <div class="px-4 py-2 rounded-lg bg-blue-500/20 text-gray-200">
                ¡Hola! Soy María, psicóloga de MenteSana. ¿En qué puedo ayudarte hoy?
              </div>
            </div>
            <img src="https://media.cssninja.io/shuriken/avatars/9.svg" alt="Avatar del psicólogo" class="w-8 h-8 rounded-full order-1">
          </div>
        </div>
      </div>
      <div class="p-4 border-t border-slate-700 bg-slate-800">
        <div class="flex rounded-lg overflow-hidden">
          <input type="text" id="chat-input" placeholder="Escribe tu mensaje..." class="flex-1 p-2 bg-slate-700 text-white focus:outline-none">
          <button id="send-message" class="bg-blue-500 text-white px-4 flex items-center justify-center hover:bg-blue-600 transition-colors cursor-pointer">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
            </svg>
            <span class="sr-only">Enviar</span>
          </button>
        </div>
      </div>
    `;
    
    document.body.appendChild(chatModal);
    
    // Mostrar notificación después de 20 segundos
    setTimeout(() => {
      const notification = document.querySelector('.chat-notification');
      if (notification) {
        notification.classList.remove('hidden');
      }
    }, 20000);
    
    // Eventos del chat
    chatButton.addEventListener('click', () => {
      chatModal.classList.remove('scale-0');
      chatModal.classList.add('scale-100');
      chatModal.setAttribute('aria-hidden', 'false');
      
      // Ocultar notificación al abrir el chat
      const notification = document.querySelector('.chat-notification');
      if (notification) {
        notification.classList.add('hidden');
      }
    });
    
    // Cerrar chat
    document.getElementById('close-chat')?.addEventListener('click', () => {
      chatModal.classList.remove('scale-100');
      chatModal.classList.add('scale-0');
      chatModal.setAttribute('aria-hidden', 'true');
    });
    
    // Enviar mensaje
    const chatInput = document.getElementById('chat-input') as HTMLInputElement;
    const sendMessage = document.getElementById('send-message');
    const chatMessages = document.getElementById('chat-messages');
    
    // Valores por defecto para el usuario
    let userName = '';
    let avatar = '/img/logo/logo.png'; // Cambiar por avatar del usuario
    
    function addMessage(message: string, sender: 'user' | 'assistant') {
      if (!chatMessages || !message.trim()) return;
      
      const messageElement = document.createElement('div');
      messageElement.className = `chat-message ${sender} mb-4`;
      
      let avatar, bgColor, textAlign, order;
      
      if (sender === 'user') {
        avatar = '/img/logo/logo.png'; // Cambiar por avatar del usuario
        bgColor = 'bg-blue-600';
        textAlign = 'items-end';
        order = 'order-1';
      } else {
        avatar = 'https://media.cssninja.io/shuriken/avatars/9.svg';
        bgColor = 'bg-blue-500/20';
        textAlign = 'items-start';
        order = 'order-2';
      }
      
      messageElement.innerHTML = `
        <div class="flex items-end ${sender === 'user' ? 'justify-end' : ''}">
          <div class="flex flex-col space-y-2 text-sm max-w-xs mx-2 ${order} ${textAlign}">
            <div class="px-4 py-2 rounded-lg ${bgColor} text-gray-200">
              ${message}
            </div>
          </div>
          <img src="${avatar}" alt="Avatar" class="w-8 h-8 rounded-full ${sender === 'user' ? 'order-2' : 'order-1'}">
        </div>
      `;
      
      chatMessages.appendChild(messageElement);
      chatMessages.scrollTop = chatMessages.scrollHeight;
    }
    
    // Función para manejar el envío de mensajes
    function handleSendMessage() {
      if (!chatInput || !chatInput.value.trim()) return;
      
      const message = chatInput.value;
      addMessage(message, 'user');
      chatInput.value = '';
      
      // Simular respuesta automática después de 1 segundo
      setTimeout(() => {
        const responses = [
          "Entiendo cómo te sientes. ¿Podrías contarme un poco más sobre esa situación?",
          "Gracias por compartir eso conmigo. Es normal sentirse así en estas circunstancias.",
          "Estoy aquí para ayudarte. ¿Desde cuándo has estado experimentando esto?",
          "¿Te gustaría agendar una sesión para hablar más en profundidad sobre esto?",
          "Vamos a trabajar juntos en esto. ¿Has intentado alguna técnica de relajación?"
        ];
        const randomResponse = responses[Math.floor(Math.random() * responses.length)];
        addMessage(randomResponse, 'assistant');
      }, 1000);
    }
    
    // Eventos para enviar mensajes
    sendMessage?.addEventListener('click', handleSendMessage);
    
    chatInput?.addEventListener('keypress', (e) => {
      if (e.key === 'Enter') {
        handleSendMessage();
      }
    });
  }
</script>
